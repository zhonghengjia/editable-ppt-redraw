# Changes

## 3.4.0 — 2026-09-09 — source support and structural evidence

- Reviewed 21 related GitHub implementations, excluding a 3D mesh-only result; retained pinned ImageTracerJS/Pixelmatch instead of adding models or changing threshold semantics.
- Added source-frozen visible-support selectors with source/mask/recipe hashes, every component/hole and explicit uniform-fill approximation. No hole filling, fragment omission or hidden-surface inference.
- Extended the existing regional contract with missing/extra support, IoU, exact discrete bidirectional boundary distances and complementary-connectivity topology. Original color gates remain mandatory.
- Added adversarial regressions for narrow gaps, lost branches, shifts, extra regions, color/structure separation, deterministic native trace candidates and fail-closed evidence/complexity limits.

## 3.3.0 — 2026-09-08 — text and path clearance

- Replace presence/typography-only label acceptance with coordinated text placement and path clearance; retain source text, hierarchy and route semantics.
- Integrate conservative native textbox/path checks into the existing validator and quality runner. Dense text-bearing PPTX without clearance evidence is unverified.
- Separate actual paragraph/run fonts from imported shape-level defaults, and numeric protected-box clearance from rendered glyph visibility.
- Regression tests cover crossing lines, arrowheads, thick strokes, fully covering paths, duplicate/missing selections and unsupported geometry. No new dependency or Git publication.

## 3.2.0 — 2026-09-08 — host-surface reconstruction

- Replace independent-part-only construction with explicit host/detail ownership, shared projected coordinates, source boundary landmarks and occlusion ordering.
- Integrate native relationship auditing into the existing manifest validator and quality runner; preserve separate geometry, pixel and manual visual results.
- Add regression cases for wrong ownership, detached or shifted details, foreground order, concave-host escape, invalid evidence and unsupported geometry.
- Independent user-requested retests start from the raw reference, not previous builders or deck components. Existing source tracing and renderer dependencies remain unchanged. No Git publication implied.

## 3.1.0 — 2026-09-08 — source-specific component fidelity

- Replace recognition-only completion for faithful scientific components with source-observed inner geometry, view, topology and occlusion requirements; distinguish this from semantic rebuilding.
- Bundle pinned, unmodified ImageTracerJS and Pixelmatch cores with licenses and source hashes. Add bounded local component tracing, native SVG preflight and provenance; no new model or hosted service.
- Add source-pixel regional contracts and final-artifact/render/source hash checks to the manifest and QA runner. Report local and worst-window differences; missing evidence stays unverified. Manual semantic review remains separate.
- Correct paragraph readback to use real newlines instead of literal backslash-n separators, with actual PPTX regression coverage.
- Decisions follow inspection of 23 related repositories' key implementations. The source figure/current PPT are unchanged; this entry does not imply a Git commit, push or release.

## 3.0.0 — 2026-09-08 — local upgrade candidate

- Replace curve-name aggregation and first-match-only checks with slide/object/path identity and per-instance comparison. Apply axis-aligned group transforms and flips; explicitly reject unsupported geometry and require component selection for multi-path shapes.
- Preserve crop-height amplitude in trace schema 2; record source hashes, interpolation evidence and configurable coverage/gap thresholds. Failed extraction does not emit a certified complete curve. Legacy trace schema 1 remains peak-normalized evidence.
- Return source primitive to actual PPTX object mappings from the native SVG helper without changing its supported SVG profile or vendored implementation.
- Replace unbounded text substring checks with token-aware, object-local matching and optional inventory page/name/ID/count scopes.
- Add per-run typography evidence, source-justified bounded exceptions and explicit unresolved inherited-font status alongside role-ratio checks.
- Scope draw.io IDs per graph model; validate explicit draw.io and active Excalidraw references.
- Separate named geometry from directed endpoint evidence. Add the unified local QA runner with transparent applicability and no inferred visual sign-off.
- Keep installed dependencies, upstream vendor code/assets, source figures and diagram-family preservation unchanged. No new model, OCR stack or web application.

This candidate follows a 24-repository README/license/core-code review. The earlier 2.7.0 release remains in Git history; this entry does not imply a commit, push or release of 3.0.0.
